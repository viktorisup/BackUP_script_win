#----------------------------------------------------
# путь к файлу или папке который необходимо бекапить
src_path = C:\ubn

# адрес smb сервера
ip_srv = 192.168.6.5

# корневая папка на smb сервере
smb_root_dir = Soft

# путь к директории относительно корневой папки smb сервера (директория должна существовать например \dir),
# если копируем в корневую папку в данном поле пишем None
smb_base_dir = None

# логин от smb сервера
login = ivc

# пароль от smb сервера
passwd = Mgmsu495!

#-----------------------------------------------------
# Если хотим бекапить несколько каталогов или файлов или вообще еще на один сервер
# просто дублируем и проставляем переменным порядковый номер (закоментированные строчки игнорируются)

# путь к файлу или папке который необходимо бекапить
src_path2 = C:\ubn\Привет

# адрес smb сервера
ip_srv2 = 192.168.6.5

# корневая папка на smb сервере
smb_root_dir2 = IVC-Doc

# путь к директории относительно корневой папки smb сервера (директория должна существовать например \dir),
# если копируем в корневую папку в данном поле пишем None
smb_base_dir2 = None

# логин от smb сервера
login2 = ivc

# пароль от smb сервера
passwd2 = Mgmsu495!

# эта строчка нужна для коректного парсинга